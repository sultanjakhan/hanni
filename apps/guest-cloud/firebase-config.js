// firebase-config.js — overwritten by scripts/firebase-setup.sh.
//
// PASTE OUTPUT OF `firebase apps:sdkconfig WEB` HERE AFTER RUNNING THE
// SETUP SCRIPT. The script writes this file automatically; this stub is
// only present so `firebase deploy --only hosting` doesn't 404.

window.HANNI_FIREBASE_CONFIG = null;
