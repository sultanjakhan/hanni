> Дополняет корневой ~/hanni/CLAUDE.md — здесь только специфика Python/MCP-сервера.

# hanni-mcp — Python MCP server

## Style
- PEP 8: 4-space indent, snake_case
- Тип-хинты на сигнатурах публичных функций

## Окружение
- venv уже есть: `desktop/hanni-mcp/.venv/` — использовать его, не ставить пакеты глобально
- Новая зависимость → добавить в `requirements.txt`, не только установить

## MCP-инструменты
- Новый tool — по образцу существующих определений в `server.py`, не изобретать паттерн
- Описания инструментов на английском

## База данных
- hanni-mcp делает CRUD по той же SQLite, что и Tauri-приложение
- Изменения схемы (новые колонки/таблицы) — это Rust-миграции в `src-tauri/`, НЕ здесь

## Verify
- После правок: `python3 -m py_compile server.py`
