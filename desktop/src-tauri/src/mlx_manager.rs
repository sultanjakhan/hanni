// MLX disabled — see memory archive/project_llm_disabled.md (Qwen3.5-35B недостаточно умна).
// Включить обратно — вернуть полный mlx_manager + start_mlx_server из git history (до 2026-04-25).

pub fn ensure_mlx() -> bool {
    false
}
