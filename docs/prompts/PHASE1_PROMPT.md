# Phase 1: Project → Modules — Module Discovery & Mapping

> **Проект:** [PROJECT_NAME]
> **Корень:** [PROJECT_ROOT]
> **Цель:** Полное сканирование проекта, выделение логических модулей, создание карты файлов.
> **Выход:** `docs/MODULE_MAP.md` + `docs/architecture/COVERAGE.md` + `docs/architecture/DISPUTED.md`

---

## Контекст

Phase 1 — первый шаг иерархии **Проект → Модули → Функции → Подфункции**.
Этот промпт определяет **какие модули существуют** в проекте и **какие файлы** (или секции файлов) к ним относятся.

**Два типа проектов:**
- **Multi-file**: модуль = группа файлов (например, `server/routes/auth/*` → модуль `auth`)
- **Monolithic**: модуль = секция внутри большого файла (например, строки 100-500 в `lib.rs` → модуль `memory`)

Промпт обрабатывает оба случая.

---

## ШАГ 0: Разведка проекта

### 0.1 — Структура каталогов

```bash
# Получи дерево проекта (без node_modules, target, .git, dist, build)
find . -type f \
  -not -path "*/node_modules/*" \
  -not -path "*/.git/*" \
  -not -path "*/target/*" \
  -not -path "*/dist/*" \
  -not -path "*/build/*" \
  -not -path "*/.next/*" \
  -not -path "*/vendor/*" \
  -not -name "*.lock" \
  -not -name "package-lock.json" \
  -not -name "Cargo.lock" \
  | sort
```

Запиши:
- Общее количество файлов
- Основные каталоги верхнего уровня
- Какие расширения преобладают (`.ts`, `.rs`, `.py`, `.go`, etc.)

### 0.2 — Стек и точки входа

Определи:

| Параметр | Значение |
|----------|----------|
| Язык(и) | [e.g., Rust, TypeScript, Python] |
| Фреймворк(и) | [e.g., Tauri, React, Express, Django] |
| Архитектура | [monorepo / monolith / microservices] |
| Файл-тип | [multi-file / monolithic / hybrid] |
| Entry points | [main.rs, index.html, server.ts, etc.] |
| Конфиг-файлы | [package.json, Cargo.toml, pyproject.toml, etc.] |

### 0.3 — Определи scope сканирования

Выбери **исходные файлы** для анализа (исключая сгенерированное, конфиги, ассеты):

```bash
# Пример для Rust + JS проекта:
find . -name "*.rs" -o -name "*.js" -o -name "*.ts" -o -name "*.tsx" \
  -o -name "*.py" -o -name "*.go" -o -name "*.html" -o -name "*.css" \
  | grep -v node_modules | grep -v target | grep -v dist | sort
```

Запиши → `SOURCE_FILES[]` с `wc -l` для каждого.

---

## ШАГ 1: Определение модулей

### Стратегия A: Multi-file проект

Если файлы организованы по каталогам (routes/, components/, services/, etc.):

1. **Группируй по каталогам**: файлы в одной папке обычно принадлежат одному модулю
2. **Группируй по import-графу**: файлы, которые импортируют друг друга, вероятно в одном модуле
3. **Группируй по домену**: файлы с одинаковыми ключевыми словами (auth, job, user) → один модуль

### Стратегия B: Monolithic проект

Если основная логика сосредоточена в 1-3 больших файлах:

1. **Прочитай файл целиком** (по частям, если > 500 строк)
2. **Выпиши все публичные функции/структуры** с номерами строк
3. **Группируй по домену**: функции работающие с одной сущностью → один модуль
4. **Зафиксируй диапазоны строк**: модуль X = строки A-B в файле Y

### Стратегия C: Hybrid (monolithic + satellite files)

Комбинация: один большой файл + вспомогательные. Примени A для satellite файлов, B для ядра.

### 1.1 — Сбор доменных сигналов

Для определения границ модулей используй сигналы:

| Сигнал | Вес | Пример |
|--------|-----|--------|
| Каталог/папка | Высокий | `routes/auth/` → модуль auth |
| Имя файла | Высокий | `memory.service.ts` → модуль memory |
| Имя функции/структуры | Средний | `fn create_event()` → модуль calendar |
| Секция комментариев | Средний | `// === MEMORY SYSTEM ===` → модуль memory |
| Общая сущность БД/таблицы | Высокий | все функции работающие с таблицей `events` → calendar |
| Import-кластер | Средний | файлы импортирующие друг друга → один модуль |
| Route prefix | Высокий | `/api/auth/*` → модуль auth |

### 1.2 — Составь список модулей-кандидатов

```markdown
## Модули-кандидаты

| # | Модуль | Описание (1 предложение) | Сигналы |
|---|--------|--------------------------|---------|
| 1 | auth | Аутентификация и авторизация | каталог routes/auth, файлы auth.*, OAuth логика |
| 2 | memory | Система памяти (запомнить/вспомнить/забыть) | функции remember/recall/forget, таблица memory |
| ... | ... | ... | ... |
| N | infrastructure | Общие утилиты, конфиги, middleware | не домен, а cross-cutting |
```

### 1.3 — Валидация: каждый файл/секция в одном модуле

Правила:
- **Каждый исходный файл** (или секция monolithic-файла) принадлежит **ровно одному** модулю
- Если файл логически относится к двум модулям → помести в **основной** (по main import), второй запиши в DISPUTED
- Если файл не относится ни к одному домену → модуль `infrastructure`
- Если непонятно → `unassigned` (разберём позже)

---

## ШАГ 2: Маппинг файлов → модули

### 2.1 — Для multi-file проектов

Создай таблицу привязки:

```markdown
| Файл | Строки | Модуль |
|------|--------|--------|
| server/routes/auth/auth.routes.ts | 642 | auth |
| server/routes/auth/oauth.routes.ts | 312 | auth |
| server/services/job.service.ts | 415 | jobs |
```

### 2.2 — Для monolithic проектов

Создай таблицу привязки по секциям:

```markdown
| Файл | Секция / Функции | Строки (от-до) | LOC | Модуль |
|------|-----------------|----------------|-----|--------|
| lib.rs | struct MemoryEntry, remember(), recall(), forget(), search_memory() | L100-L350 | 251 | memory |
| lib.rs | create_event(), get_events(), delete_event(), sync_calendar() | L351-L600 | 250 | calendar |
| main.js | renderChatView(), handleSend(), streamResponse() | L50-L500 | 451 | chat_ui |
```

### 2.3 — Контрольная сумма

```
Всего исходных файлов: [X]
Распределено по модулям: [Y]
Не распределено: [X-Y]

Проверка: сумма LOC всех секций/файлов по модулям = общий LOC проекта
```

Если не сходится → найди пропущенное.

---

## ШАГ 3: Создание выходных документов

### 3.1 — MODULE_MAP.md

```markdown
# [PROJECT_NAME] — Module Map

> Quick lookup: файл/секция → модуль.
> Авто-генерация Phase 1. Обновляется при Phase 2.

## Модули

| # | Модуль | Описание | Файлов/секций | LOC |
|---|--------|----------|---------------|-----|
| 1 | auth | Аутентификация и авторизация | 5 | 1200 |
| 2 | memory | Система памяти ИИ | 3 | 800 |
| ... | ... | ... | ... | ... |
| **TOTAL** | | | **[X]** | **[S]** |

## Маппинг по путям

### [Layer 1] (e.g., Backend / Rust)

| Файл | Секция (если monolith) | Строки | Модуль |
|------|----------------------|--------|--------|
| ... | ... | ... | ... |

### [Layer 2] (e.g., Frontend / JS)

| Файл | Секция (если monolith) | Строки | Модуль |
|------|----------------------|--------|--------|
| ... | ... | ... | ... |

### [Layer 3] (e.g., Scripts / Config)

| Файл | Строки | Модуль |
|------|--------|--------|
| ... | ... | ... |
```

### 3.2 — COVERAGE.md

```markdown
# [PROJECT_NAME] — Coverage Tracker

> Master-реестр всех файлов/секций проекта. Каждый элемент приписан к модулю.
> Гарантия полноты: сумма всех модулей + unassigned = total.

## Общая статистика

| Метрика | Значение |
|---------|----------|
| Всего элементов (файлов/секций) | [X] |
| Распределено по модулям | [Y] |
| Не распределено | [X-Y] |
| Покрытие | [Y/X * 100]% |

## Счётчик по модулям

| Модуль | Элементов | LOC | Статус Phase 1 | Статус Phase 2 |
|--------|-----------|-----|----------------|----------------|
| auth | 5 | 1200 | ✅ mapped | ⬜ pending |
| memory | 3 | 800 | ✅ mapped | ⬜ pending |
| ... | ... | ... | ... | ... |
| **unassigned** | **?** | **?** | ❓ | — |
| **TOTAL** | **[X]** | **[S]** | — | — |

## Полный список

### [Layer 1]

| Файл | Секция | Строки (от-до) | LOC | Модуль | Статус |
|------|--------|----------------|-----|--------|--------|
| ... | ... | ... | ... | ... | ✅/⬜/❓ |

### [Layer 2]

| ... | ... | ... | ... | ... | ... |

## Статусы

- ✅ — распределён в модуль
- ⬜ — известен, ещё не обработан
- ❓ — не ясно какому модулю принадлежит (→ DISPUTED.md)
- 🔴 — orphan (кандидат на удаление)
```

### 3.3 — DISPUTED.md

```markdown
# [PROJECT_NAME] — Disputed Items

> Спорные принадлежности, cross-cutting код, нераспределённые элементы.
> Обновляется при каждой фазе.

## Спорные элементы (принадлежат >1 модулю)

| # | Файл/секция | Назначенный модуль | Альтернативный модуль | Причина спора | Статус |
|---|-------------|-------------------|----------------------|---------------|--------|

## Нераспределённые

| # | Файл/секция | LOC | Возможный модуль | Статус |
|---|-------------|-----|------------------|--------|

## Кандидаты на рефакторинг

| # | Что | Почему | Приоритет | Статус |
|---|-----|--------|-----------|--------|
```

---

## ШАГ 4: Валидация и самопроверка

### 4.1 — Целостность маппинга

```
✅/❌ Каждый исходный файл есть в COVERAGE.md
✅/❌ Сумма LOC по модулям = общий LOC
✅/❌ Сумма элементов по модулям + unassigned = total
✅/❌ Каждый элемент в MODULE_MAP.md есть в COVERAGE.md
✅/❌ Нет файлов, назначенных двум модулям
```

### 4.2 — Качество декомпозиции

Проверь, что модули обладают свойствами:
- **Cohesion**: элементы модуля связаны по домену (не по слою)
- **Naming**: имя модуля отражает бизнес-домен, не техническую деталь
- **Size**: ни один модуль не содержит >40% кода (иначе разбей)
- **Minimum**: не менее 3 модулей (иначе слишком грубо)
- **Infrastructure**: cross-cutting код вынесен в `infrastructure` / `core`

### 4.3 — Специальная проверка для monolithic файлов

Если есть файлы >1000 строк, для каждого проверь:
- Все ли публичные функции/структуры назначены модулю?
- Нет ли «серых зон» (диапазоны строк без модуля)?
- Совпадают ли границы секций (без перекрытий и пробелов)?

---

## ПРАВИЛА

1. **`wc -l` перед описанием.** Не пиши про файл, пока не посчитал строки.
2. **Читай перед маппингом.** Не назначай модуль файлу, пока не прочитал хотя бы его exports/структуру.
3. **Один элемент = один модуль.** Спорные → DISPUTED.md, но назначь основной.
4. **Контрольная сумма обязательна.** LOC и count должны сходиться.
5. **Не изобретай модули заранее.** Только то, что видишь в коде. Нет кода → нет модуля.
6. **Infrastructure — не свалка.** Только реально cross-cutting код (утилиты, конфиги, типы). Если можно отнести к домену → отнеси.
7. **Granularity подбирай по проекту.**
   - Маленький проект (<20 файлов) → 3-5 модулей
   - Средний (20-100 файлов) → 5-12 модулей
   - Большой (100+) → 10-20 модулей

---

## ВЫХОДНЫЕ ФАЙЛЫ

```
docs/
├── MODULE_MAP.md                    — карта: файл/секция → модуль
└── architecture/
    ├── COVERAGE.md                  — master-реестр + статистика
    └── DISPUTED.md                  — спорное + нераспределённое
```

---

## ПОСЛЕ ЗАВЕРШЕНИЯ

Покажи:

1. **Список модулей** с кратким описанием и LOC
2. **Покрытие**: `[Y]/[X] элементов распределено ([P]%)`
3. **Top-3 крупнейших модуля** (по LOC)
4. **Спорные**: сколько записей в DISPUTED.md
5. **Контрольная сумма**: `auth([A]) + memory([B]) + ... + unassigned([U]) = [X] total` ✅/❌
6. **Monolithic alert** (если есть): какие файлы >1000 строк и на сколько модулей разбиты

Спроси:
- "Модули определены верно? Хочешь переименовать/разбить/объединить?"
- "Переходим к Phase 2 (декомпозиция на функции) для модуля [САМЫЙ_КРУПНЫЙ]?"
